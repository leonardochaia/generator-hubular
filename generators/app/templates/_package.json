{
  "name": "hubular-app",
  "version": "1.0.0",
  "private": true,
  "author": "<%= botOwner %>",
  "description": "<%= botDescription %>",

  "scripts": {
    "prebuild": "rimraf hubular-scripts",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "start": "yarn build && yarn hubular",
    "start:debug": "yarn build && yarn hubular --debug"
  },
  "engines": {
    "node": ">=8"
  }
}
